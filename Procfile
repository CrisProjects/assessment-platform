# ¡ATENCIÓN!
# Este archivo Procfile en la raíz NO debe usarse para despliegue en Render.
# Usa el Procfile dentro de /backend para producción.
#
# Para desarrollo local puedes usar:
# web: python backend/app.py
#
# Para Render, configura el root directory como 'backend' y usa el Procfile de ese directorio.

web: gunicorn app:app
