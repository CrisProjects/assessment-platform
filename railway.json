{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "gunicorn wsgi_production:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-file -",
    "healthcheckPath": "/api/status",
    "healthcheckTimeout": 100
  }
}
