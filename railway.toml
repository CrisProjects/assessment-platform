[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT wsgi_production:application"
healthcheckPath = "/api/status"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
PYTHONUNBUFFERED = "1"
FLASK_ENV = "production"
