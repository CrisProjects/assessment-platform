[variables]
PYTHONPATH = "/app"

[phases.build]
cmd = "pip install -r requirements.txt"

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[start]
cmd = "gunicorn wsgi_production:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-file -"
