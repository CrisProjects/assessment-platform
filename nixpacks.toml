# Railway deployment - Force Python 3.11 without mise
[providers]
python = "3.11"

[phases.setup]
nixPkgs = ["python311", "libpq"]

[phases.install]
cmds = ["pip3.11 install --upgrade pip", "pip3.11 install -r requirements.txt"]

[start]
cmd = "python3.11 wsgi_production.py"
