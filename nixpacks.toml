# Railway Python deployment - Configuración simplificada
providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Building Python Flask application'"]

[start]
cmd = "python wsgi_production.py"

[variables]
PYTHON_VERSION = "3.11"
FLASK_ENV = "production"
PYTHONUNBUFFERED = "1"
